#include <ACPI.hpp>
#include <Console.hpp>

extern "C" void Entry64(void) {
    ACPI acpi;

    //uint16_t *vmem = (uint16_t *)(0xB8000);
    if (acpi.Initialize()) {
        //*vmem = 0x4040;
    }
    else {
        //*vmem = 0x6060;
    }
}